node_modules/
package-lock.json
/test-results/
/playwright-report/
/run-*.json
/blob-report/
/playwright/.cache/
/vendor/
composer.lock
.phpunit.result.cache

# Local verification artifacts (Playwright MCP snapshots + preview screenshots)
.playwright-mcp/
/*.png

# Disposable local WordPress test instances (foundation wp-test-env.mjs).
# .wp-smoke is the second one, kept for upgrade checks that need a site with
# real saved content on the previous version.
.wp-test/
.wp-smoke/

.design-source/
